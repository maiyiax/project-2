{{!-- provide information on plant and allow user to change plant room --}}

<div class="post card">
  <div class="card-header">
    <h2>Edit Plant</h2>
  </div>

  <div class="card-body">
        {{> description plant}}
  </div>

  <form id="edit-post-form" class="card-body">
    <input type="hidden" name="post-id" value="{{plant.id}}" />

    <label class="form-label" for="post-title">Title</label>
    <input type="text" name="post-title" value="{{post.title}}" class="form-input" />

    <label class="form-label" for="post-body">Content</label>
    <textarea name="post-body" class="form-input">{{post.body}}</textarea>

    <button type="submit" class="btn">Update</button>
  </form>
</div>

<script src="/javascript/edit-plant.js"></script>