<div class="post card">
    <div class="card-header">
      <h2>{{common_name}}</h2>
    </div>

    <div class="card-body">
      <div><img src="{{plant.image_url}}" alt="{{plant.scientific_name}}"/></div>
      <div><p>{{plant.description}}</p></div>
    </div>
  </div>

